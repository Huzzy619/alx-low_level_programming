#include "main.h"
/**
  *print_square - print squares
  *@n: indicating size of the square
  *Return: Always 0
  */

void print_square(int n)
{
	int i = 0;
	int y = 0;

	if (n > 0)
	{
		while(i < n)
		{
			while(y < n)
			{
				_putchar('#');
				y++;
			}
			b = 0;
			a++;

			_putchar('\n');
		}
	}
	else
		_putchar('\n');
}

